{
  "name": "note",
  "description": "A minimal html editor",
  "type": "module",
  "main": "src/note.mjs",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "build-dev": "./node_modules/.bin/esbuild src/note.mjs --bundle --format=esm --sourcemap --outfile=www/app.js",
    "build": "./node_modules/.bin/esbuild src/note.mjs --bundle --format=esm --sourcemap --outfile=www/app.min.js --minify",
    "watch": "watch 'npm run build-dev' ./src & npx http-server www/"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/poef/note.git"
  },
  "author": "auke@muze.nl",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/poef/note/issues"
  },
  "homepage": "https://github.com/poef/note/#readme",
  "devDependencies": {
    "esbuild": "^0.28.1",
    "eslint": "^10.5.0",
    "jest": "^30.4.2",
    "watch": "^1.0.2"
  },
  "dependencies": {
  }
}
